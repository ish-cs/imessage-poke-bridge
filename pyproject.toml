[project]
name = "imsg-poke-mcp"
version = "0.1.0"
description = "Local MCP server wrapping the imsg CLI so Poke can read/send iMessages"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "rumps>=0.4.0",
]
